import { createClient } from '@supabase/supabase-js'; import fs from 'fs'; const supabase = createClient(process.env.VITE_SUPABASE_URL, process.env.VITE_SUPABASE_SERVICE_ROLE_KEY); const sql = fs.readFileSync('supabase-migration/23-upgrade-documents.sql', 'utf8'); async function run() { console.log('Running SQL...'); const statements = sql.split(';').map(s => s.trim()).filter(s => s.length > 0); for (const s of statements) { console.log('Executing:', s.substring(0, 50) + '...'); const { error } = await supabase.rpc('exec_sql', { sql: s }); if (error && !error.message.includes('Could not find the function')) { const { error: e2 } = await supabase.from('exec_sql').select('*').limit(1); console.log(error); } else if (error && error.message.includes('Could not find the function')) { console.log('No exec_sql RPC found. Please run the SQL manually in Supabase Dashboard.'); break; } } console.log('Done.'); } run();
